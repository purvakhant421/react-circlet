import React from 'react'

const Data = (data) => {
    return (
        <div>
            <h2>My fist name is {data.first}</h2>
            <h2>My Surname is {data.last}</h2>
        </div>
    )
}

export default Data
